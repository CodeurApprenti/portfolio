{% extends 'base.html.twig' %}

{% block title %}{{ pin.title }}{% endblock %}

{% block body %}
    <article>
        <h1>{{ pin.title }}</h2>
        <p>{{ pin.description}}</p>
     
    </article>

    <a href="{{ path('app_home') }}"> Go Back</a>

{% endblock %}
